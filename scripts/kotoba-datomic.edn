{:kotoba.datomic/name :common-crawl/ingest
 :kotoba.datomic/level :l0-operator-script
 :kotoba.datomic/rebuild-runbook "../MIGRATION-TODO.md"
 :kotoba.datomic/owner "common-crawl maintainers"
 :kotoba.datomic/last-reviewed "2026-06-03"
 :kotoba.datomic/canonical-sources
 [:org.commoncrawl/warc
  :org.commoncrawl/wat
  :org.commoncrawl/wet]
 :kotoba.datomic/tx-target
 :com.etzhayyim.apps.kotoba.datomic/transact
 :kotoba.datomic/entities
 [:common-crawl/domain
  :common-crawl/page
  :common-crawl/link]
 :kotoba.datomic/deprecated-substrates
 [{:kind :risingwave
   :binding "DATABASE_URL"
   :note "Legacy operator ingest path; migrate emitted rows to kotoba datomic EDN transactions."}]
 :kotoba.datomic/rebuild-estimated-minutes 480
 :kotoba.datomic/rebuild-note "Dominated by Common Crawl segment download and parse time."
 :kotoba.datomic/non-determinism
 [{:field :db/id
   :reason "Datomic entity ids are allocated at transact time; replay preserves lookup refs and logical identity."}
  {:field :common-crawl/observed-at
   :reason "Operator ingest wall-clock timestamps are replay-specific unless supplied by source metadata."}]
 :kotoba.datomic/promotion
 {:required
  ["Emit .kotoba.edn tx data from each ingest script before DATABASE_URL writes are removed."
   "Replay a bounded Common Crawl sample into kotoba datomic in CI."
   "Drift detector compares sampled datoms against source WARC/WAT/WET records."]
  :target-quarter "Q3 2026"}}
